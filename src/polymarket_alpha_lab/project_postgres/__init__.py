"""Project-owned native PostgreSQL: no Docker, Supabase services or shared DB.

Import has no side effects. Use ProjectPostgres from .server with an explicit
project root; runtime installation and initialization are separate actions.
"""
